# การนำขึ้น Streamlit Cloud

## สิ่งที่ต้องอัปโหลด

`app.py` กับโฟลเดอร์ `advisor/` **ต้องขึ้นไปพร้อมกันเสมอ** อัปโหลดแค่ `app.py`
อย่างเดียวจะทำให้แอปพังทั้งหน้า เพราะ `app.py` เรียกใช้ฟังก์ชันที่อยู่ในไฟล์
`advisor/` ซึ่งบนเซิร์ฟเวอร์ยังเป็นของเก่า

```
app.py                       ← หน้าแอปหลัก
requirements.txt             ← มี reportlab และ kaleido
advisor/                     ← ทั้งโฟลเดอร์ 21 ไฟล์ รวม __init__.py
assets/fonts/                ← Sarabun-Regular.ttf, Sarabun-Bold.ttf, OFL.txt
data/Fund Return.xlsx        ← ข้อมูลผลตอบแทนกองทุน
.streamlit/config.toml       ← ถ้ามี
```

ไม่ต้องอัปโหลด: `test_app.py`, `data/client_notes.json` (บันทึกของ RM เป็นราย
deployment ไม่ใช่ source), `__pycache__/`, `.DS_Store`

## ถ้าอัปโหลดไม่ครบ

แอปจะไม่พังเงียบ ๆ อีกแล้ว — หน้าแรกจะขึ้นข้อความ **"ไฟล์บนเซิร์ฟเวอร์ไม่ตรงกับ
app.py"** พร้อมรายชื่อไฟล์ที่ต้องอัปโหลดใหม่และฟังก์ชันที่ขาด เพราะ Streamlit
Cloud ปิดบังข้อความ error จริง (`redacted to prevent data leaks`) ทำให้
traceback บอกแค่บรรทัดที่พัง ไม่บอกสาเหตุ

## ฟอนต์ภาษาไทย

`assets/fonts/` ต้องขึ้นไปด้วย ไม่งั้น PDF ที่ export จะเป็นกล่องสี่เหลี่ยม
ทั้งหน้า — ฟอนต์มาตรฐานของ PDF ทุกตัวมีแต่อักษรละติน และ **เรนเดอร์ภาษาไทย
เป็นกล่องเงียบ ๆ โดยไม่ error** ถ้าโฟลเดอร์นี้หาย ปุ่ม export จะขึ้นข้อความบอก
แทนที่จะสร้างไฟล์เสีย

## เรื่อง CPU

Streamlit Cloud จำกัด CPU และจะ throttle ถ้าใช้หนักต่อเนื่อง ส่วนที่กินแรงมาก
ที่สุดในแอปนี้:

| ส่วน | ค่า default | หมายเหตุ |
|---|---|---|
| Monte Carlo | 10,000 path | ลดเหลือ 2,500 ได้ผลใกล้เคียงมากสำหรับการคุยกับลูกค้า |
| Efficient Frontier | คำนวณทุกครั้งที่เปิดแท็บ 2 | cache แล้ว แต่ยังหนักตอนเปลี่ยนเงื่อนไข |
| กราฟใน PDF (kaleido) | เฉพาะตอนกดปุ่ม | เปิด Chromium เบื้องหลัง ใช้ RAM ~200MB |

ถ้าโดน throttle บ่อย ให้ลด default ของ Monte Carlo ลงก่อน เป็นจุดที่ได้ผลมาก
ที่สุดต่อการแก้หนึ่งจุด และถ้าเครื่องปลายทางมี RAM จำกัดจริง ๆ การถอด
`kaleido` ออกจาก requirements จะทำให้ export PDF ยังใช้ได้ แต่จะไม่มีกราฟ
(ช่องติ๊กกลุ่มกราฟจะถูกปิดพร้อมบอกเหตุผลเอง)
